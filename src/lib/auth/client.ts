/**
 * Browser Better Auth client used by the first-party login, signup, password
 * reset, and sign-out flows.
 */

import { createAuthClient } from "better-auth/react";

export const authClient = createAuthClient();

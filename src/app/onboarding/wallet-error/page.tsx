import InvalidWalletError from "../../../views/InvalidWalletError";

/** Route: /onboarding/wallet-error — reference for the inline invalid-address validation state. */
export default InvalidWalletError;

import Marketing404 from "../views/Marketing404";

/**
 * Next.js App Router special file — auto-rendered for any unmatched route
 * or explicit `notFound()` call. No need to link to it manually.
 */
export default Marketing404;

import ComponentStatesSheet from "../../../views/ComponentStatesSheet";

/** Route: /dev/component-states — specimen sheet for toasts, modals, and system states. Delete before shipping. */
export default ComponentStatesSheet;
